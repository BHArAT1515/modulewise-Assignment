"You can use the random.choice() function with range() to pick a random item from a range."

import random

random_number = random.choice(range(1, 11))
print("Randomly Selected Number:", random_number)
